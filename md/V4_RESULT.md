# v4 결과 — span v3 계약 달성

작성 2026-08-02. v3 는 `../memcached-1.6.42-port_v3/` 에 동결돼 있다.

```text
계약   1:10 혼합(SET:GET) 10 M ops/s  AND  GET-only 10 M ops/s
       두 워크로드 모두 GET span < 30 µs  AND  SET span < 30 µs
       span 정의는 v3 — backend 진입에서 응용 가시 완료까지
```

---

## 최종 게이트

재검 두 라운드(§7·§10)와 sweep 재측정(§8·§9·§12)이 끝난 뒤 값이다.
**각 120 초**, 추적기 1 개, 아무것도 동승하지 않음.

```text
GET-only  13.214 M   span 26.32 = adm 3.67 + v2 22.65                  ✓ ✓
1:9 혼합  10.899 M   GET 19.62 = 7.00 + 12.61                          ✓ ✓
                     SET  8.77 = 0.51 + 7.40 + 0.86                    ✓ ✓
```

**두 워크로드 모두 처리량과 span 을 만족한다.**

운영값:

```text
ext_admit_max=64  ext_reap_every=12  ext_post_chain=8  ext_setq_max=1
ext_submit_inline  ext_submit_batch=20  W=24  nqp=4  mcT=30  -R 1024
                                        └─ §12 에서 40 → 24
```

여유(계약선 대비):

```text
혼합 처리량  +9.0%    ← 구속 지표
GET-only     +32%
GET span     26.32 대 30 → 12.3%
혼합 span    GET 34%,  SET 71%
```

**게이트가 예측과 맞다.** §12 의 cap 24 셀들로 `13.258 / 10.880` 을 미리
적어두고 쟀는데 `13.213 / 10.899` 가 나왔다 — 둘 다 0.4% 안이다.

개별 창이 아니라 분포로 보면(§11) 혼합은 게이트선보다 **6.5σ** 위다.
`13.405 / 10.777` 같은 최고값은 분포의 위쪽 끝이지 기대값이 아니다.

재검 전 120 초 게이트(참고, 창 두 번):

```text
GET-only  12.393 M / 25.05    SET-only 5.434 M / 7.67
1:9 혼합  10.122 M / G 20.41  S 21.68        10.094 M / G 20.45  S 21.70
```

출발점 대비:

```text
              혼합       GET span   SET span
v3 정의 직후   9.83 M     316.24     188.75     둘 다 위반
v4 최종       10.90 M      19.62       8.77     둘 다 통과
```

**GET span 16 배, SET span 22 배 감소. 처리량 +10.9%.**

---

## 1. 무엇이 문제였나 — 대기 세 겹

span 정의를 v2 → v3 로 넓히자 계약이 8 배 초과로 깨졌다. **정의가 성능을 바꾼
것이 아니라 원래 있던 대기가 계측 안으로 들어왔다.** 증거: 클라이언트가 보고한
SET 지연이 7.45 ms 인데 v2 는 7.8 µs 라고 했다.

v3 로 GET 에 추가된 구간은 전부 **워커 전용 로컬 작업**이다 — TLS 캐시 할당,
io_cache 할당, 빈 iov, 구조체 복사, 큐 삽입, 카운터 증가. 200 ns 면 끝날 일에
25 µs 가 붙어 있었다:

| 대기 | 크기 | 원인 | 수정 |
|---|---:|---|---|
| 제출 | 9.91 µs | 연결의 읽기 버퍼를 다 파싱해야 io_queue 제출 | 파싱 즉시 post |
| 완료 관측 | 31.56 → 5.52 | CQE 가 pass 끝까지 **폴링조차 안 됨** | post 자리에서 수거 |
| SET 재개 | 25.48 µs | 거두고도 재개를 다음 pass 로 미룸 | 마지막 재개만 보류 |

**SET 이 답을 갖고 있었다.** pac 의 admit 은 처음부터 0.5~0.7 µs 였다 —
큐에 넣자마자 그 자리에서 flush 하기 때문이다. GET 경로만 상류 memcached 의
io_queue 배칭을 물려받고 있었다.

## 2. 처리량은 락에서 나왔다

span 을 연 뒤 혼합은 9.2 M 이었다. 나머지는 전부 **경합**이었다:

| 수정 | 무엇이었나 | 효과 |
|---|---|---|
| 아이템 카운터 샤딩 | SET 1 건이 공유 캐시라인을 **10 번** 갱신. stub 이 전부 같은 slab class | SET-only +27% |
| refcount 원자화 | GET 이 `item_lock` 을 **두 번** 잡음. 두 번째는 `--refcount` 가 원자가 아니어서 | 두 워크로드 +8.5% |
| 워커 통계 락 제거 | GET 1 건이 `stats.mutex` 를 **두 쌍** 잡음 | 혼합 +0.6% → 계약 |

셋 다 **원자 연산이거나 락이 필요 없는 데이터인데 30 워커가 같은 줄을
두드리던 것**이다. 프로파일에서 `pthread_mutex` 가 계속 1 위였고, 세 번에
걸쳐 `C_get` 2.754 → 2.469, 락 0.504 → 0.291 로 내려갔다.

---

## 3. 코드 변경

```text
3a50784  SET 반환 경로 — 완료를 거둔 그 pass 에서 재개
185ec64  badcrc — 원격 슬롯 회수를 unlink 에서 item_free 로
3edbd11  ext_submit_inline — 파싱 즉시 post, io_queue 우회
9d4a924  post 자리 수거 — 파싱 중인 연결과 자기 자신을 제외
adb82a0  item_trylock — 락 보유를 추측하지 않고 질의
460d326  마지막 재개만 보류 — resps_suspended > 1 이면 안전
30ae0a4  ext_reap_every — 수거를 N 건마다로 상각
6277c37  ext_post_chain — post 를 N 건씩 묶어 QP 스핀락을 1/N 로
c580468  ext_setq_max 를 stats settings 에 노출
b75a61d  아이템 카운터를 워커별로 샤딩
6ba0c55  refcount 원자화 — item_remove 가 0 일 때만 잠근다
d88ea82  워커 통계 뮤텍스 제거
```

### badcrc — v4 이전부터 있던 정합성 결함

혼합에서 **있는 키가 조용히 MISS 로 돌아가고 있었다**(pipe=256 에서 GET 의
0.025%). 상류 extstore 는 페이지 단위로 재활용하고 `page_version` 으로 낡은
읽기를 걸러내는데, **이 포트는 슬롯 단위로 재활용하면서 버전을 올리지
않는다**(`extstore.c:534`). `storage_delete` 가 unlink 시점에 loc 을 회수해
읽는 중인 원격 메모리를 다음 SET 이 덮어썼다.

회수를 `item_free` 로 옮겼다 — GET 이 이미 refcount 를 쥐므로 그것으로 보호가
성립한다. 호출처가 9 군데라 `STORAGE_delete` 매크로 한 곳에서 막았다.

**우리 둘 다 못 봤다**: `err5` 집계에 badcrc 가 없었고 hit 율 0.025% 는
"100%" 로 반올림된다. v3 의 계약 달성치도 이 조건에서 잰 값이다.

---

## 4. 측정하고 기각한 것

| 시도 | 결과 |
|---|---|
| EVP 배관 | 마이크로벤치 최선 −3.1% → CPU/op 의 0.34% |
| `item_lock` 배열 축소 (15→10) | **−4% 악화** |
| IPoIB connected 모드 | 커널이 거부 (`I/O error`) |
| SWIOTLB 바운스 | op 당 ~400 B ≈ 40 ns |
| rx-usecs coalescing | 처리량 불변 (이미 패킷당 1 인터럽트) |
| `-R` 32 | 처리량·span 둘 다 악화 |
| `ext_setq_max` 4 | 혼합 +3%, 두 span 초과. SET 밀도 10% 라 배치를 못 채운다 |
| `admit_max` + `chain` 조합 | 상한이 체인 슬롯을 세어 과도하게 조인다 |
| `mcT` 16 (물리 코어 독점) | 6.56 M. vCPU 당 효율 +21% 지만 코어 수가 이긴다 |
| flush 를 락 밖으로 | 혼합 9.05 대 9.18. SET span 은 8.6 으로 좋아지나 처리량을 낸다 |
| `reap` 16 (refcount 후) | 처리량 불변, GET span 31.16 초과. 같은 손잡이가 상태에 따라 반대 |

---

## 5. 남은 여유와 다음 표적

혼합 여유는 최종 게이트에서 **+9.0%** 다. 아래는 `C_get 2.469`(§7 이전) 기준의 남은
항목이고, `resp_allocate` 는 §7 에서 처리했다:

```text
[k] __irqentry   0.241   9.8%   NIC 인터럽트. coalescing 은 기각됨
락 (item_lock)   0.291  11.8%   조회 1 쌍. 해시 버킷과 합치면 줄지만 큰 변경
해시             0.173   7.0%   assoc_find + prefetch + murmur
resp_allocate    0.123   5.0%   ← §7 에서 처리 (memset 1192 → 164 B)
EVP 배관         0.120   4.9%   기각됨
평문 목적지 할당  0.012   0.5%   기각 — 크기가 없다
```

**남은 것 중 크기가 있는 건 `item_lock` 조회 한 쌍(0.291)과 커널 인터럽트
(0.241)뿐이다.** 전자는 락 배열을 해시 버킷과 합쳐야 줄고(큰 변경), 후자는
coalescing 이 이미 기각됐다.

기계 한계도 적어둔다:

```text
호스트  AMD EPYC 9124  물리 16 코어 / 32 스레드
guest   -smp 30 (관리자 확정, 변경 없음)
        vCPU 는 호스트 0..29 에 1:1 핀. SMT 짝 (0,16)…(15,31)
        → 14 코어가 SMT 공유, 2 코어 단독

물리 코어 1 개 단독 0.410 M/s,  SMT 2 스레드 0.604 M/s  (배율 1.47)
현재 배치의 천장 = 14×0.604 + 2×0.410 = 9.27 M  ← 이 값은 개선 전 기준이다
```

**절대값은 낡았지만 비율은 살아남았다.** §9 에서 같은 1.47 배율로 세운 모형이
`mcT` 축 네 점을 1% 안에서 맞힌다 — 천장을 SMT 가 정한다는 결론은 그대로고,
현재 빌드의 천장이 13.3 M 으로 올라갔을 뿐이다.

---

## 6. 재현

```text
서버   mcT = 30   -m 2048 -c 16384 -R 1024   taskset -c 0-29
       ext_worker_window=24  ext_qp_per_worker=4  ext_drain_spin=1024
       hashpower=22  ORD=0(협상→16)  총 QP = 120
       ext_admit_max=64  ext_reap_every=12  ext_post_chain=8
       ext_setq_max=1  ext_submit_inline  ext_submit_batch=20
       ext_loc_mag_depth=64  ext_pac_set=on  ext_seal_at_flush=off
환경   MLX5_COHERENT_QP=1 MLX5_COHERENT_CQ=1 EXT_RDMA_PROF=1 EXT_SELFTEST=1
       EXT_SLOT_SIZE=256  EXT_READ_SLOTS=64
부하   mtT=30  -c 4 (120 커넥션)  pipeline=256  -d 64  --test-time=120
       --key-prefix=m- --key-minimum=1 --key-maximum=1000000
       --key-pattern=R:R --distinct-client-seed
키공간 1,000,000 × 64 B

무장   AD=64 RE=12 PC=8 SQ=1 INLINE=1 tools/exp1-arm.sh 20 24 4 64
슬라이스 python3 tools/exp0-slice.py <trace.csv>
```

**측정 위생**: 추적기(`shape-trace.sh`)는 **하나만** 띄운다. 캠페인 중 6 개가
누적돼 후반 셀이 낮게 편향된 적이 있다. perf 는 guest 디스크가 6.8 G 뿐이므로
`-F 199`, 콜그래프 없음, 10 초(약 4 MB)로 뜬다.

세는 법도 조심해야 한다. `pgrep -f shape-trace` 는 **자기 명령줄을 같이
잡아** 항상 하나 더 센다 — 그 때문에 없는 중복을 있다고 읽은 적이 있고,
`pkill -f` 로는 자기 자신을 죽인 적도 있다. 대괄호로 끊는다:

```bash
pgrep -c -f 'shape-trace-v[3].sh'    # 자기 명령줄엔 v[3] 이라 안 맞는다
```

perf 는 커널 버전을 확인해야 한다. guest 는 `6.16.0-snp-guest` 인데
`/usr/bin/perf` 는 6.8 래퍼라 거부한다. 실제로 도는 것은
`/usr/lib/linux-tools/6.8.0-111-generic/perf` 다. **새 계측 스크립트는 창에
걸기 전에 스모크로 샘플 수를 확인한다** — stderr 를 버린 채 조용히 실패해서
창 하나를 통째로 날린 적이 있다.

raw 는 `experiments/` 아래. EXP-0 은 `exp0-20260801/FINDINGS.md`,
EXP-1 축별 판정은 `EXP1_FINDINGS.md`, 프로파일은 `prof-20260802/`.


---

## 7. 재검 — 계약 달성 뒤 추가로 찾은 것

관리자 요청으로 여유를 다시 뒤졌다. 둘을 찾았다.

```text
                    GET-only              1:9 혼합
게이트 (재검 전)  12.389 M / 25.05     10.116 M / G 20.41  S 21.68
A-1 flush 락 밖   12.529 M / 26.34     10.125 M / G 20.59  S  8.62
MEMSET            13.405 M / 25.99     10.777 M / G 19.87  S  8.97
                   +8.2%                +6.5%
```

### A-1 — flush 를 락 밖으로 (세 번째로 부호를 바꾼 손잡이)

캠페인 중간에 시도했다가 **졌던** 것이다(혼합 9.05 대 9.18). 그때는 refcount
감소와 워커 통계 뮤텍스가 그대로였고, 락 밖으로 빼면 `storage_flush_returns`
가 trylock 을 더 자주 이겨 완료 처리를 인라인으로 더 하느라 처리량을 팔았다.
**둘을 걷어낸 뒤에는 그 일이 싸서 지연 이득이 공짜로 온다.**

`reap` · `ext_setq_max` 에 이어 세 번째다. **옛 판정은 그것이 측정된 지형이
바뀌면 살아남지 못한다.**

### MEMSET — 한 줄

```c
memset(resp, 0, sizeof(*resp));              // mc_resp 1192 B
memset(resp, 0, offsetof(mc_resp, wbuf));    //          164 B
```

`mc_resp` 는 `char wbuf[1024]` 를 품고 `wbuf` 가 마지막 멤버다. GET 1 건마다
**1028 B 를 쓸데없이 쓰고 있었다** — 12 M ops/s 면 초당 12.3 GB 다. `wbuf` 는
쓰기 전에 채워지므로 0 으로 만들 이유가 없다.

대역폭만으로는 0.068 µs 를 예상했는데 실측은 그 두 배가 넘었다. 캐시 오염까지
걷힌 것으로 본다.

### 기각한 것

```text
A-2  reap × chain 재sweep      GET-only span 여유가 3.7 µs 뿐이라 여지가 좁다
A-3  평문 목적지를 응답 버퍼로  실제 비용이 0.012 µs (C_get 의 0.5%) 였다.
     내가 resp_allocate 와 같은 그룹으로 묶어 6.2% 로 잘못 읽었다 — 그 착오
     덕에 옆에 있던 진짜(1 KB memset)를 찾았다
```

---

## 8. B-1 — pipeline 축 (v2 config-matrix 의 v4 판)

off-box, v4 코드, span v3. 14 셀.

```text
pipe        32      64     128     256     384
GET-only  7.619   9.772  12.378  13.303  13.266  M
  span    25.06   25.94   26.79   26.03   25.98
혼합      6.590   8.217  10.194  10.770  10.109  M
  Gspan   21.47   22.24   20.65   19.85   21.44
  Sspan   10.53   10.53    9.59    8.94    8.93
```

**span 이 pipeline 깊이와 무관해졌다.** EXP-0 이 같은 축을 v4 이전 코드로 재서
얻은 GET span 은 `76.4 → 78.7 → 131.9 → 242.3` 이었다 — 제시량에 비례해 자랐다.
지금은 12 배 범위에서 4% 만 움직인다. **인라인 post 와 post 자리 수거가
없앤 큐가 여기 그대로 보인다.**

`pipe=256` 이 꼭짓점인 것은 그대로지만 **계약을 만족하는 점이 하나에서 셋으로
늘었다** — 128·256·384 가 모두 혼합 10 M 을 넘는다. MEMSET 이전 표(같은 축,
`7.245 / 9.474 / 11.926 / 12.354 / 11.816`, 혼합 `… 10.049 / 8.746`)에서는
256 하나뿐이었고 384 는 13% 퇴행했다. 지금 384 의 퇴행은 GET 0.3% 다.
**운영점이 한 pipeline 값에 얹혀 있지 않다는 뜻이다.**

---

## 9. B-2 — mcT 축 (v2 sensitivity 의 v4 판)

`pipeline=256` 고정, 나머지 운영값. 8 셀.

```text
mcT   GET-only   span                혼합      GET span   SET span   코어
 20   10.457 M   21.95 = 3.46+18.49   8.365 M   17.55      7.50      20.6
 24   11.651 M   24.48 = 3.53+20.94   9.504 M   18.36      7.96      24.5
 28   12.774 M   25.48 = 3.64+21.84  10.376 M   19.37      8.66      28.2
 30   13.303 M   26.03 = 3.65+22.39  10.770 M   19.85      8.94      29.9
```

### 천장은 호스트 SMT 가 정한다 — 예측으로 확인했다

호스트는 물리 16 코어 / SMT 32 스레드고 guest vCPU 30 개가 host CPU 0..29 에
1:1 로 핀돼 있다. 짝이 `(0,16)…(15,31)` 이므로 `taskset 0..N-1` 은 `N-16` 쌍을
공유한다. 이전 코어 측정(단독 0.410 M/s, 공유 쌍 합계 0.604 M/s)에서 공유
스레드는 단독의 `0.7366` 배다. 용량을 단독 스레드 환산으로 쓰면

```text
mcT   공유  단독   환산 용량
 20     8    12   5.89 + 12 = 17.89
 24    16     8  11.79 +  8 = 19.79
 28    24     4  17.68 +  4 = 21.68
 30    28     2  20.62 +  2 = 22.62
```

`mcT=20` 과 `30` 두 점으로 모형을 세우고 **나머지 두 점을 측정 전에
예측해 채널에 박아둔 뒤** 쟀다.

```text
        GET-only 예측 / 실측      혼합 예측 / 실측
 24      11.64 / 11.651  +0.1%     9.42 / 9.504  +0.9%
 28      12.75 / 12.774  +0.2%    10.32 / 10.376 +0.5%
```

**네 점 다 1% 안이다.** 코어 열이 전부 `mcT` 와 같으니 워커는 포화고,
곡선의 모양은 전적으로 SMT 공유 비율이 만든다.

이것이 말하는 바가 축 자체보다 중요하다. **소프트웨어 경합이 이제 측정
한계 아래라는 뜻이다** — 락이 남아 있었다면 스레드를 늘릴수록 모형보다
아래로 처졌을 것이다(§2 의 세 겹을 걷기 전이 그랬다). 그러므로 **30 vCPU
고정 하에서 남은 지렛대는 op 당 CPU 하나뿐이고, 1% 줄이면 1% 오른다.**

### 이 축은 프론티어다

span 이 처리량과 같이 움직인다(21.95 → 26.03). 계약을 만족하는 점은

```text
GET-only   20 · 24 · 28 · 30  전부
혼합       28 과 30 만  (24 는 9.504 로 처리량 미달)
```

`mcT=28` 은 30 보다 span 이 0.55 µs 짧고 처리량이 3.7% 낮다. 둘 다 계약
안이므로 운영값은 여유가 큰 30 을 유지하되, **span 여유가 더 필요해지면
28 이 준비된 대안**이다.

### 잡일 스레드는 축이 아니다

24 워커 구성에서 `/proc/<pid>/task/*/stat` 를 갈랐다.

```text
mc-worker × 24   200,119 tick
memcached (main)      21
mc-log · mc-assocmaint 0
```

**워커 외 전부 0.01%** 다. LRU maintainer·crawler·slab automove 는 스레드가
아예 없다.

---

## 10. 2 차 재검 — 현재 빌드 프로파일과 표적 셋

§7 은 MEMSET·A-1 **이전** 프로파일로 골랐다. 그 뒤 처리량이 8.2% 올랐으니
분포가 달라졌을 것이라 **현재 빌드로 다시 떴다.**

`PROF MIX` 창(10.544 M) 안에서 60,782 샘플. **혼합이 구속 워크로드라 이쪽이
맞다.**

```text
 8.47%  [k] __irqentry_text_start        NIC 인터럽트 (SNP guest 진입 비용)
 5.83%  assoc_find                 ┐
 1.69%  assoc_prefetch             ├ 해시 조회 8.0%
 0.49%  MurmurHash3_x86_32         ┘
 2.93%  pthread_mutex_lock         ┐
 2.62%  pthread_mutex_unlock       ├ 뮤텍스 6.24%
 0.69%  pthread_mutex_unlock@plt   ┘
 4.04%  EVP_CIPHER_CTX_get_iv_length  ┐
 1.30%  EVP_CIPHER_CTX_ctrl           ├ EVP 6.08%
 0.74%  OSSL_PARAM_construct_size_t   ┘
 3.15%  mlx5_poll_cq_v1            ┐ CQ 폴링 5.32%
 2.17%  extstore_worker_drain      ┘
 3.37%  do_item_get / 2.93% storage_get_item / 2.79% resp_allocate
 0.60%  extstore_prof_stamp        ← 계측 자체. span 을 재려면 낸다
```

### 표적 셋 — 전부 무승부

```text
                  GET-only            1:9 혼합
기준            13.303 M            10.770 M
item_lock=17         —              10.742 M   −0.26%
CPU_RELAX       13.235 M  −0.5%     10.778 M   +0.07%
-march v3       13.236 M  −0.5%     10.821 M   +0.47%
```

세 값 다 bed drift 1.4% 안이다. **셋 다 되돌렸다** — 배포 바이너리는
`b4c18e97`, 이 문서의 모든 수치를 낸 그것이다.

`CPU_RELAX` 와 `-march` 가 GET 에서 `13.235 / 13.236` 으로 겹친다. **서로
무관한 두 변경이 같은 값을 내면 변경의 효과가 아니라 bed 가 13.30 → 13.24
로 내려앉은 것**이다. −0.5% 를 퇴행으로 읽지 않는 근거다.

### 각 표적이 왜 안 먹었나

| 표적 | 근거였던 것 | 실제 |
|---|---|---|
| `item_lock_power` | 뮤텍스 6.24% | 15→10 은 −4%, 15→17 은 평평. **크기는 지렛대가 아니다** — 경합도 배열 캐시도 아니고 락 자체다 |
| `CPU_RELAX` | 폴링 5.32% | 운영점은 워커당 체류 11.6 이라 **CQ 가 빌 틈이 없다.** 5.32% 는 빈 poll 낭비가 아니라 실제 수거 비용 |
| `-march=x86-64-v3` | `-O2` 만 쓰고 있었다 | 무승부. 포인터 추적·락·syscall 이 대부분이라 벡터 폭이 안 는다 |

**프로파일의 심볼 비중은 "줄일 수 있는 양" 이 아니다.** EVP(§4)에 이어 세
번 확인했다. 비중은 표적을 고르는 데 쓰고 **크기는 A/B 로만 확인한다.**

### 같이 닫은 것

```text
대량 clear    요청 경로 최대가 io_pending_t 176 B. §7 의 memset 두 자리가 마지막
잡일 스레드   24 워커 구성에서 워커 외 전부 합쳐 0.01%
CQ 통합       워커당 CQ 는 이미 하나고 QP 4 개가 공유한다 (extstore.c:615)
```

### 남은 것과, 왜 지금 안 하나

```text
assoc_find 8.0%   4 M 버킷(32 MB) 포인터 추적. hashpower 를 낮추면 L3 에
                  들어갈 수 있으나 탐침이 1.0 → 1.5 로 는다. B-3 에서 잰다
인터럽트 8.47%    SNP guest 의 인터럽트 진입 비용. coalescing 은 §4 에서 기각
뮤텍스 6.24%      락 배열을 해시 버킷과 합쳐야 준다. 여유 +7.8% 로 착수할
                  종류의 변경이 아니다
```

---

## 11. bed 재현성 — 델타를 읽는 해상도

이 세션에서 **같은 구성**(기준 바이너리 `b4c18e97`, `mcT=30 W=40 nqp=4
pipe=256`)을 여러 번 쟀다. 서로 다른 실험의 대조군으로 찍힌 것들이라
독립 반복으로 쓸 수 있다.

```text
GET-only  n=11   평균 13.175 M   σ 0.133 (1.01%)   범위 13.017~13.405 (2.9%)
1:9 혼합  n= 9   평균 10.732 M   σ 0.112 (1.04%)   범위 10.536~10.830 (2.7%)
```

**2σ 가 약 2.0% 다. 이보다 작은 델타는 한 셀로 구별할 수 없다.**
§10 의 표적 셋(−0.26%, ±0.5%, ±0.5%)은 전부 그 안이므로 "무승부" 가 맞고,
`CPU_RELAX` 와 `-march` 가 `13.235 / 13.236` 으로 겹친 것도 이 분포 안의
같은 자리에 떨어진 것이다.

### 계약 판정을 분포로 다시 쓰면

```text
혼합 처리량  10.732 ± 0.112 M  (n=9)
계약선       10.0 M
초과폭       +7.3%  =  6.5σ
```

**단일 최고값(10.777)을 인용하는 것보다 이쪽이 강한 진술이다.** 최고값은
분포의 위쪽 끝이지 중심이 아니다. 계약은 운 좋은 한 창이 아니라 **9 개
독립 창의 중심이 게이트보다 6.5σ 위**라서 달성된 것이다.

같은 이유로 §최종 게이트의 `13.405 / 10.777` 은 **그 창의 값**이지 기대값이
아니다. 재현을 시도하면 `13.18 / 10.73` 근처가 나오는 것이 정상이다.

---

## 12. B-3 — 동시성 축 (v2 three-exp 의 v4 판)

v2 의 `qpxdepth` 는 `W ∈ {128, 256}` 이었다. **v4 에서 그 방향은 빈 축이다.**
Little 로 운영점 체류를 구하면

```text
GET-only  13.175 M × 26.03 µs / 30 = 워커당 11.54
1:9 혼합  10.732 M × 19.85 µs / 30 = 워커당  7.13
```

상한(창 40, ORD 64)이 필요치의 3.5~5.5 배다. **올려봐야 아무것도 안 바뀐다.**
그래서 질문을 뒤집어 **내려가면서** 쟀다 — 어디서 처음 물리는가.

### 창(W) 축

```text
 W    GET-only            혼합                    혼합 span
      처리량   span(adm)   처리량      z     n    GET     SET
40   13.175   26.03(3.65) 10.732      —     9   19.85   8.94
24   13.174   26.24(3.75) 10.850   1.35σ    2   19.64   8.75   ← 채택
16   13.328   27.19(7.36) 10.950   2.49σ    2   19.55   8.78
 8   11.809   80.08(64.5) 10.720      —     1   21.11  15.13   위반
```

**벽은 8 과 16 사이, 체류 11.54 가 있는 자리다.** Little 이 맞았다.

`W=8` 은 예측대로 **두 워크로드를 갈랐다** — GET-only(11.54 > 8)는 −11.4%,
혼합(7.13 < 8)은 −0.6%. 그런데 **span 예측은 반대로 틀렸다.** "체류가 줄면
대기도 준다"고 20.35 µs 를 예상했는데 80.08 이 나왔다.

```text
창은 backend 체류만 조인다        v2 15.54 로 실제로 줄었다
못 들어간 요청은 admit 에서 기다린다  adm 3.65 → 64.54
span v3 는 그 대기를 센다          그래서 총합이 3 배로 늘었다
```

코드로 확인했다: `extstore.c:807` 이 `outstanding < window` 로 post 를 막고
초과분은 대기 목록으로 가며, `t_start` 는 실제 post 시각(`:846`),
`t_enter` 는 span v3 진입(`storage.c:569`)이다. **큐를 앞으로 옮겨도 span
v3 는 따라온다** — §1 에서 걷어낸 대기 세 겹과 같은 성질인데, 나는 그걸
알면서 v2 식 직관으로 다시 계산했다.

### 왜 `W=16` 이 아니라 `W=24` 인가

`W=16` 이 혼합 +2.03%(2.49σ)로 더 크다. 그런데 `admit` 이 3.65 → 7.36 으로
두 배가 된다 — **여유 1.4 배는 큐가 서기 시작하는 자리**이고, 그 큐가
`W=8` 에서 `adm 64.54 / span 80.08` 로 터지는 걸 직접 봤다.

```text
W=16   오늘은 통과 (27.19 < 30, 여유 9.4%). 관측된 절벽의 어깨 위다
W=24   여유 2.1 배. admit 3.75 로 무변화 — 절벽에서 떨어져 있다
```

`W=24` 는 **기존 운영값을 엄격히 지배한다**: GET-only 무변화, 혼합은
처리량·GET span·SET span 셋 다 개선. **파는 것이 없다.** `+0.9%` 를 더
벌자고 어깨에 서지 않는다. `W=16` 은 "처리량 최대" 가 필요할 때의 대안.

### nqp 축 — 그리고 두 축이 하나로 접힌 것

`nqp` 를 1·2·4·8 로 쓸었더니 처음에는 `nqp=1` 이 캠페인 최고(혼합 11.066,
GET-only 13.487)를 냈다. **재현되지 않았다** — 확인 셀에서 10.834, 10.852 /
13.091 이 나왔다. 반복을 요구한 것이 여기서 값을 했다.

그 덕에 진짜 변수가 보였다. `W` 와 `nqp` 를 따로 보지 말고
**유효 상한 `min(W, nqp × ORD)`** (ORD 는 협상값 16)으로 묶으면 전부 한
곡선에 선다:

```text
cap    n   혼합평균   기준대비      z      만든 방법
  8    1   10.720    −0.11%   −0.10σ    W=8  nqp=4
 16    5   10.930    +1.85%   +3.18σ    W=16 nqp=4  와  W=24 nqp=1
 24    4   10.880    +1.38%   +2.19σ    W=24 nqp=4  와  W=24 nqp=8
 32    2   10.768    +0.33%   +0.41σ    W=24 nqp=2
 40    9   10.732     기준             기존 운영값
```

**같은 cap 안에서 QP 수는 처리량에 아무 것도 하지 않는다:**

```text
cap 24   nqp=4 (QP 120) 10.850   nqp=8 (QP 240) 10.909   +0.54%
cap 16   nqp=4 (QP 120) 10.950   nqp=1 (QP  30) 10.917   −0.30%
```

QP 총수를 30 에서 240 까지 **8 배** 흔들어도 안 움직인다. 이것이 기전 추정
둘을 죽였다:

```text
"post chain 이 QP 수만큼 쪼개진다"   `worker_post`(extstore.c:789)가 배치
                                     하나로 QP 하나를 겨냥하고 ORD 가 배치를
                                     제한한다 — nqp=1 이면 오히려 작아진다
"NIC 의 QP 컨텍스트 캐시"            120 → 30 으로 줄여도 무변화. 반증됐다
```

**왜 상한이 낮으면 나은지는 아직 설명이 없다.** in-flight 바운스 슬롯의
작업집합(`cap × slot_size` = 4 KB 대 10 KB per worker)이 유력하지만
측정하지 않았다. **무엇이 변수인지만 확정됐다.**

### 운영값 — `W` 를 40 에서 24 로

`cap 16` 대 `cap 24` 는 **`+0.47%`, t=1.02 (df=7) 로 구별되지 않는다.**
(캠페인 중 양쪽이 n=2 였을 때는 `cap 16` 이 이기는 것처럼 보였고 실제로
그렇게 판정했다가, n 이 5·4 로 늘면서 차이가 사라졌다.)

구속 지표가 동률이면 다음 지표로 정한다 — GET-only span 이다:

```text
cap 16   span 27.4    admit 7.4     상한이 물려 큐가 선다
cap 24   span 26.24   admit 3.75    안 물린다
```

```text
운영값   W = 24  (nqp=4 그대로)
효과     혼합 +1.38% (z=2.19σ).  GET-only 처리량·span 무변화
```

**바꾸는 것이 한 줄이고 파는 것이 없다.**

부수 관찰 하나: 같은 cap 이라도 QP 가 적으면 **혼합 span 이 낮다**
(`W=24 nqp=1` 의 v2 12.11 대 `W=16 nqp=4` 의 12.57, 4%). "cap 만 중요하다"
는 처리량에 한해 참이다. 둘 다 30 µs 에서 멀어 판정에는 안 썼다.

### hashpower — 해시 8.0% 를 겨냥한 마지막 공짜 시험

```text
              GET-only            혼합
hp=22 (기준)  13.258 M           10.880 M
hp=20         12.761 M  −3.8%    10.603 M  −2.5%
```

버킷을 4 M → 1 M 로 줄이면 배열이 32 MB → 8 MB 라 캐시 잔류를 사지만,
부하율이 0.24 → 1.0 이 되어 탐침이 1.0 → 1.5 로 는다. **탐침이 이겼다.**

`assoc_find` 8.0% 는 프로파일의 최대 소프트웨어 항목이지만 **이 손잡이로는
못 줄인다.** 줄이려면 자료구조를 바꿔야 한다.

주: `hp=20` 세 번째 셀(`혼합 10.976`)은 **내가 재기동 창에 겹쳐 오염시켰다.**
집계에서 뺐다. 판정은 오염 전 두 셀로 한 것이고 둘 다 같은 방향 2σ 밖이다.

---

## 13. 재검 결산

두 라운드에서 시험한 전부다.

```text
✅ A-1  flush 를 락 밖으로     SET span 21.68 → 8.62,  처리량 +1%
✅ MEMSET                      GET 당 1028 B 절약.  +8.2% / +6.5%
✅ W 40 → 24                   혼합 +1.38% (z=2.19σ), 파는 것 없음

⛔ item_lock_power 17          −0.26%.  크기로는 뮤텍스를 못 줄인다
⛔ CPU_RELAX (pause)           ±0.5%.  운영점에선 CQ 가 빌 틈이 없다
⛔ -march=x86-64-v3            ±0.5%
⛔ hashpower 20                −3.8% / −2.5%
⛔ EVP 배관                    다 걷어도 5.2 ns (C_get 의 0.2%)
⛔ 대량 clear 재점검           남은 최대가 io_pending_t 176 B
⛔ 잡일 스레드                 워커 외 전부 0.01%
⛔ CQ 통합                     이미 워커당 하나를 QP 들이 공유
⛔ QP 수 30~240                처리량 무영향
⛔ reap × chain 재sweep        span 여유가 좁아 미실시
```

### 배운 것 — 프로파일 비중은 줄일 수 있는 양이 아니다

`EVP`, `CPU_RELAX`, `item_lock_power`, `hashpower` 넷 다 **프로파일에서 크게
보여서 골랐고 넷 다 무승부이거나 퇴행**이었다.

```text
EVP 6.08%          다 걷어도 0.2%     — 호출 자체가 비용이지 배관이 아니다
폴링 5.32%         무승부             — 빈 poll 낭비가 아니라 실제 수거
뮤텍스 6.24%       크기로 안 됨       — 경합도 배열 캐시도 아니고 락 자체
해시 8.0%          퇴행               — 탐침과 캐시가 맞바꿈, 이미 최적 근처
```

**비중은 표적을 고르는 데 쓰고, 크기는 A/B 로만 확인한다.**

### 그리고 이긴 셋은 프로파일이 가리킨 곳이 아니었다

```text
A-1       지형이 바뀌어 옛 판정이 뒤집힌 것
MEMSET    `resp_allocate` 5.0% 를 잘못 읽다가 옆에서 발견한 것
W 40→24   Little 로 상한이 안 문다는 걸 알고 내려가며 쓸어서 찾은 것
```

셋 다 **모형(체류·지형·구조)에서 나왔지 심볼 순위에서 나오지 않았다.**

---

## 14. 전수 sweep 캠페인 — 117 셀

관리자 지시로 **건너뛴 축을 전부** 측정했다. "목표를 충족하지 못하더라도
데이터로서 의미가 있으니 일단 다 측정해."

### 14-1. `reap` × `chain` — 건너뛰려던 축에서 가장 큰 발견

`chain` 축 9 점(reap=12):

```text
chain  GETthr  GETspan   adm    혼합thr  혼합GET    SET     계약
   1   11.851   17.64   0.12    9.793    12.81   10.57    ✗ 혼합 미달
   2   12.853   19.12   0.76   10.504    13.98    9.75    ✓
   4   13.152   20.62   2.02   10.804    16.25    8.80    ✓  무릎
   8   13.191   26.32   3.75   10.866    19.62    8.75    ✓  배포값
  12   13.740   31.17   6.89   11.132    28.84   11.65    ✗ GET span
  16   13.783   31.02   6.85   11.162    28.80   11.62    ✗
  20   13.791   31.36   6.89   11.163    28.73   11.55    ✗
  24   13.714   31.25   6.91   11.104    28.84   11.56    ✗
  32   13.682   31.36   6.96   11.131    28.78   11.54    ✗
```

**계약 창은 `2 ≤ chain ≤ 8`, 처리량 무릎은 4, 12 이상은 완전 포화다.**
포화는 실효 체인 길이를 **설정이 아니라 한 pass 의 도착 수**가 정하기
때문이다.

`reap` 축 8 점(chain=4):

```text
reap    1     2     4     6     8    10    12    14
GETspan 5.60  8.31 12.12 16.96 17.49 20.14 20.62 23.14
혼합thr 9.40✗ 10.15 10.68 10.73 10.80 10.69 10.80 10.82
```

**`reap` 4~14 는 처리량이 전부 같고 span 만 다르다** — span 을 공짜로 고를
수 있는 구간이 이만큼 넓고, **배포값 12 는 그 안에서 거의 최악**이었다.

### 14-2. 구조 — 두 손잡이가 span 의 두 구간을 나눠 쥔다

`storage.c:607` 이 **reap 틱 안에서 `pending_chain_flush()` 를 부른다.**
그래서 `reap < chain` 이면 체인이 차기 전에 비워진다:

```text
adm  ←  유효 체인 = min(chain, reap)     6 점에서 확인
v2   ←  reap 이 지배하되 chain 도 들어온다
```

후자는 처음에 `v2 ← reap` 로 단순화했다가 **`r14c12` 가 반증**했다
(예측 span 27~29, 실측 36.04). 격자 전체로 보면 `reap` 을 고정한 각 행이
평평하지 않고 폭이 1.96 → 5.99 µs 로 자란다.

### 14-3. pipeline — span 이 부하와 무관하다

```text
pipe      8     16     32     64    128    256    384
GETthr  3.462  5.347  7.541  9.869 12.403 13.287 13.343
GETspan 25.40  25.42  24.84  26.05  26.89  26.24  26.16
adm      5.83   5.58   4.43   4.61   3.88   3.70   3.73
```

처리량이 3.9 배 오르는 동안 **span 은 8% 폭**이다. EXP-0 이 v4 이전
코드로 `76.4 → 242.3` 을 얻었던 축이다. **span 은 `reap`·`chain` 이 쥐고
pipeline 은 그 둘을 안 건드린다.**

`adm` 이 **저부하에서 더 크다**(5.83 대 3.70). 배칭이 건수 기준이고
**시간 기준 타임아웃이 없어서** 도착이 느리면 그 건수를 채우는 데 오래
걸린다. 계약은 고부하 기준이라 문제는 없지만 설계 성질이다.

### 14-4. v1 의 세 질문

**① 총량인가 형태인가** — `nqp × ORD = 16` 고정, 분배만 변경:

```text
셀   nqp  ORD  총QP   GETthr  GETspan   adm    혼합thr   계약
A1    1    16    30   13.361   27.90   7.94   10.907    ✓
A2    2     8    60   13.220   27.00   7.41   10.837    ✓
A3    4     4   120   12.951   20.83   4.19   10.795    ✓
A4    8     2   240   12.230   21.70   4.36   10.336    ✓
A5   16     1   480   11.472   23.45   4.91    9.699    ✗
```

**처리량은 형태에 단조 감소**(−14.1%, v3 의 −12% 와 같은 부호),
**span 은 U 자**로 `nqp=4` 가 바닥이다 — **지금 운영값이 U 자의 바닥**이다.

**② depth=1 에서 어디까지 선형인가** — `ORD=1`, `nqp` 만 증가:

```text
in-flight  총QP   GETthr   v2    체류/워커  상한대비
    1       30    5.574   4.83     0.90      90%
    2       60    7.976   6.28     1.67      83%
    4      120    9.301  10.11     3.13      78%
    8      240   10.292  17.21     5.90      74%
   16      480   11.486  18.93     7.25      45%
   32      960   11.144  19.06     7.08      22%
```

**in-flight 8 까지는 창이, 16 이상은 CPU 가 묶는다. 고원 11.5 M.**
v2 캠페인의 "depth=1 천장 0.65 M" 이 **17 배** 올랐다.

**`B1` 의 `v2` = 4.83 µs 가 이 캠페인의 가장 값진 숫자다** — 큐잉이 전혀
없는 완료 1 건당 순수 비용이다. 운영점 `v2` 22.65 중 **78% 가 큐잉**이라는
뜻이고, 남은 여지의 크기가 처음으로 수치로 잡혔다.

**③ 천장이 부하인가 처리 능력인가** — `mtT = mcT` 동반 확장:

```text
mcT   mtT=30 (§9)   mtT=mcT   차이
 20     10.457       10.491   +0.3%
 24     11.651       11.523   −1.1%
 28     12.774       12.812   +0.3%
```

**세 점 다 σ 안 — 천장은 처리 능력이고 `mtT=30` 은 전 구간 과잉이었다.**
`mcT=16` 을 현재 빌드로 처음 재서 **9.279 M**(개선 전 6.56 대비 +41%)이다.

### 14-5. frontier 와 stock 대조

원 실험(`FRONTIER_7POINT_20260724.md`)의 네 형태를 재현했다.
**stock 대조는 그 문서가 기록한 `97ceee04…` 와 같은 바이너리**다.

```text
셀    구성                원본 처리량   v4      원본 spanv2  v4 v2구간
F1'   nqp4 ORD16 pipe4     1.851      1.902     28.519      17.74
F2    nqp8 ORD16 pipe8     2.445      3.243     22.252      20.27
F3    nqp8 ORD16 pipe6     2.146      2.649     21.636      17.50
F4    nqp6 ORD16 pipe4     1.779      1.936     19.077      17.74
```

`F1` 의 `depth=32` 는 **하드웨어가 거부한다** — HCA `max_qp_rd_atom` 이 16
이라 `initiator_depth=32` 로는 `rdma_cm connect failed: Invalid argument`
이 나고 서버가 안 뜬다. 16 으로 낮춰 `F1'` 로 돌렸다.

**로컬 메모리 대비:**

```text
pipe   Port      stock     비율      원본 비율
   4   1.902     2.185    87.0%      93.65%
   6   2.649     3.076    86.1%      81.42%
   8   3.243     3.882    83.5%      72.54%
 256  13.287    16.417    80.9%        —      ← 운영 조건
```

**운영 조건에서 이 포트는 로컬 메모리의 80.9% 를 낸다.** 값을 원격 메모리에
두고 one-sided RDMA 로 읽으며 AES-256-GCM 으로 봉인·복호하고 SEV-SNP guest
안에서 도는 대가가 **19.1%** 다. 원 실험이 gap 을 지적한 `pipe=8` 구간은
72.5% → 83.5% 로 11 포인트 좁혔다.

(stock 도 10~17% 올랐다 — guest 가 24 → 30 vCPU 다. **비율로만 비교한다.**)

### 14-6. 이 캠페인이 반증한 내 주장들

```text
"chain=4 는 무승부"         처리량만 보고 span 을 안 쟀다. 실제 span −21.7%
"reap·chain 재sweep 불필요"  건너뛰려던 축에서 최대 발견이 나왔다
"v2 ← reap"                 r14c12 이 반증
"min(chain, reap, ORD)"      A4 가 반증 — ORD 는 배치 길이만 정한다
"NIC QP 컨텍스트 캐시"        QP 를 30~960 으로 흔들어도 처리량 무영향
예측 밴드 ±0.2%              σ 를 재놓고 안 썼다. ±2σ 로 고친 뒤 대부분 적중
```

**여섯 중 다섯은 "전부 측정하라"는 지시가 없었으면 안 잡혔다.**
기각을 논증으로 하면 그 논증이 틀렸을 때 알 방법이 없다.

원장은 `experiments/full-20260803/cells.csv` 에 117 행이다.
