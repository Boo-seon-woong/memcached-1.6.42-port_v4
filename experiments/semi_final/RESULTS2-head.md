# semi_final 결과 2 — 라운드 5 (수정 빌드 전면 재측정)

측정: 2026-08-07~. 구성마다 **YCSB 워크로드 3종**을 각 **60초**, 사이 20초.

```text
YCSB-C  --ratio=0:1    read 100%
YCSB-B  --ratio=1:19   read 95% / update 5%
YCSB-A  --ratio=1:1    read 50% / update 50%
```

> 비율만 YCSB 를 따른다. YCSB 본래 정의는 요청 분포도 Zipfian 이지만
> memtier 에 Zipfian 이 없어 **키 분포는 uniform**(`--key-pattern=R:R`)이다.

빌드 `memcached.permr` sha `b7fe29841a6c04ff45708347` 하나로 전 격자를 돌린다.
명세는 `PLAN.md`. 라운드 3(180초, 빌드 `c11ede3e`)의 결과는 `RESULTS.md`.

## 0. 이 문서가 RESULTS.md 와 다른 점

**측정된 지표를 선별하지 않고 전부 싣는다** (관리자 지적 2026-08-07).
`RESULTS.md` 는 값 크기·ORD 축에만 span 을 적고 pipeline·chain 축에는
처리량만 실었다 — 추적기가 42열을 다 내는데 내가 임의로 골라 담은 것이다.
여기서는 축마다 세 표를 낸다.

```text
① 처리량과 read 측 span 분해  Mops get_s busy Gv3_avg/p50/p99
                              Gadmit Gv2 Gxfer Gcrypto Gsync
② update 측 span 분해         Sv3_avg/p50/p99 Sadmit Sv2 Sret Sxfer Scrypto
③ 클라이언트 계층·유효성      srv srv_p50 srv_p99 que bk
                              secs set_s err5 badcrc hit_pct
```

표는 `tools/r5-tables.py` 가 `rows.tsv` 에서 생성한다 — 손으로 옮겨 적지
않으므로 본문과 표가 어긋날 수 없다. 아직 안 돈 셀은 `미측정` 으로 남는다.

## 1. 왜 라운드 3 값을 그대로 쓰지 않는가

라운드 3 은 세 결함 위에서 돌았고 둘은 **측정값을 바꿨다**(`PLAN.md` §0).
운영점 동일 조건에서 수정 전후 실측:

| 라운드 3 워크로드 | 쓰기 비중 | 라운드 3 (c11ede3e) | 수정 후 (c33d13f9) | 차이 |
|---|---:|---:|---:|---:|
| GET | 0% | 12.873 M | 12.874 M | +0.01% |
| MIX | 10% | 10.687 M | 11.181 M | +4.62% |
| SET | 100% | 5.635 M | 6.350 M | +12.70% |

개선폭이 쓰기 비중에 단조 비례한다. READ 는 ORD 로 묶여 큐를 얕게 쓰지만
WRITE 는 ORD 면제라 깊게 쓴다(`extstore.c:972`) — 과대한 SQ/CQ 비용을 쓰기
경로가 다 물고 있었다. 실패 계수기는 전 항목 0 이었으므로 일이 줄어서 오른
것이 아니다.

따라서 **라운드 3 의 읽기 전용 값은 살아 있고 쓰기가 섞인 값은 전부 눌렸다.**
(라운드 3 은 GET/MIX(1:9)/SET 이었고 라운드 5 는 YCSB C/B/A 라 워크로드
정의 자체가 달라, 두 라운드의 쓰기 포함 수치는 직접 비교하지 않는다.)
게다가 큐 크기가 `2 × nqp² × ORD` 로 커져 nqp·ORD 가 변한 Q·O·S 축은 축
내부까지 교란됐다. 한 빌드에서 전 격자를 다시 재는 것 외에 일관된 표를
만들 방법이 없다.

## 2. 라운드 3 대비 달라진 격자

```text
5번 nqp     6점 (Q64 복구)    워커별 MR 등록으로 nqp=64 무장 확인
6번 ORD     5점 (변화 없음)   ORD 는 QP 속성, max_qp_rd_atom=16 이 전부
7번 형태    5점 (방향 전환)   곱 256 을 ORD 상향이 아니라 nqp 상향으로 만든다
                              16×16 32×8 64×4 128×2 256×1 — 발자국 전부 동일
3번 값크기  9점 (변화 없음)   slot=256 유지라 d ≤ 128
```

7번 축의 다섯 점은 `slots 512`, 워커당 bounce 128 KB 로 **발자국이 완전히
같다.** 라운드 3 의 형태 판정을 무효로 만들었던 교란이 구조적으로 없다.

## 3. 판정 기준

라운드 3 에서 동일 구성 4쌍(OP↔Q4, OP↔OP-r3, O4↔O4-r2, Q16↔S16x16)이
0.45~1.10% 였다. 재현성을 **약 1%** 로 보고 그 위에서 판정한다.

```text
1% 미만    구분 안 됨
1~2%       복제 필요 (닫지 않는다)
2% 이상    효과
```

라운드 5 자체의 드리프트 가드는 `R5-OP` ↔ `R5-OP-r2` 다.

## 4. 진행 상태

라운드 5 는 68구성 204부하, 약 6시간이다. 아래 표는 완료된 셀부터 채워지며,
`미측정` 이 남아 있으면 아직 도는 중이다. 분석과 결론은 격자가 다 찬 뒤에
이 절 아래에 붙인다 — 부분 데이터로 미리 결론을 쓰지 않는다.

---

## 5. 전 축 측정표
