# 실험 결과 해설 — semi_final 라운드 6

이 문서는 **무엇을 발견했고 그것이 무엇을 뜻하는가**를 설명한다.
수치 표 전체는 `experiments/semi_final/RESULTS2.md` 에 있고(측정 42열 전부),
측정 조건은 `experiments/semi_final/PLAN.md` 에 있다.

---

## 0. 무엇을 재고 있나

memcached 가 **값을 원격 메모리에 두고 one-sided RDMA READ 로 가져오는** 포트다.
서버는 AMD SEV-SNP 게스트 안에서 돌고, 값은 `genie_memd`(10.99.0.2)에 있다.
아이템 헤더는 로컬, 값은 원격이라 **GET 한 번이 RDMA READ 한 번**이다.

라운드 6 격자: **70구성 × 6부하 = 420부하**, 각 60초.

```text
워크로드   YCSB-C  read 100%      YCSB-B  read 95%/update 5%
           YCSB-A  read 50%/update 50%
키 분포    U  uniform            Z  zipfian θ=0.99 (YCSB ScrambledZipfian)
변인 9개   pipeline · thread · value_size · chain · nqp · ORD ·
           형태(곱 256) · client×pipeline(곱 1024) · 키 분포
```

측정 신뢰도: 캠페인 시작·중간·끝 가드 3점이 **12.807 ~ 12.971** (폭 1.3%),
7시간 드리프트 0.52%. 읽기 전용 재현성은 0.40%.

---

## 1. 가장 큰 발견 — **편중된 쓰기에는 절대 천장이 있다**

zipfian + YCSB-A(쓰기 50%)에서 처리량이 **약 1.6 M ops/s 에 붙는다.**
서버 손잡이를 무엇으로 돌려도 움직이지 않는다.

```text
값 크기 4→128 B (32배)   1.594 ~ 1.641     ← 데이터 이동량 32배인데 3%
체인 1→16                1.562 ~ 1.656
형태 16×16→256×1         1.607 ~ 1.658
스레드 8→28              2.182 → 1.734     ← 스레드를 3.5배 줘도 안 오른다
```

같은 조건에서 **uniform 은 계속 오른다**(스레드 8→28 에서 3.530 → 7.348).
즉 "zipfian 이 몇 % 손해" 가 아니라 **"zipfian 은 여기서 멈춘다"** 가 맞는
서술이다. 흔히 인용되는 `Z/U = 0.23` 은 분모만 커져서 생기는 착시다.

### 왜 천장인가

**값 크기가 무관하다는 것이 결정적이다.** 대역폭이나 암복호가 병목이면
값이 32배일 때 천장도 움직여야 한다. 안 움직인다 → **연산당 직렬화**다.

수치가 잠금 모델과 맞는다.

```text
천장 1.62 M · 1위 키 점유 6.5% · 쓰기 절반
  → 그 키 하나에 쓰기 53 K/s
  → 직렬화라면 임계구역 19.0 µs
실측 SET span (uniform 운영점) 33.7 µs — 같은 자릿수
```

코드가 뒷받침한다. 쓰기 경로는 `item_lock(hv)` 아래에서 돌고
(`storage.c:621`, `:826`), 완료까지 스핀한다(`storage.c:1216`
`while (!wait.done) drain`). **item lock 을 원격 왕복 내내 쥐면** 핫 키
하나의 처리량이 `1/왕복지연` 으로 묶이고, 그것이 전체 천장이 된다.

> 개선 방향이 명확하다 — **원격 왕복을 락 밖으로 빼는 것.**
> 락 보유 구간에 스핀이 실제로 들어가는지는 코드로 더 확인해야 한다.

### 그런데 편중 자체는 비용이 아니다

스레드가 적을 때(mcT 1~2) **zipfian 이 uniform 보다 빠르다**(Z/U 1.05~1.13).
핫 키가 캐시에 남아 지역성 이득이 난다. 즉 skew 는 공짜이거나 이득이고,
**경합이 켜지는 임계를 넘는 순간 천장으로 바뀐다.**

---

## 2. 배치(chain)는 처리량과 지연을 **둘 다** 개선한다

`ext_post_chain` 은 한 번에 몇 개를 묶어 post 하느냐다.

| chain | Mops | span | admit | v2 |
|---:|---:|---:|---:|---:|
| 1 | 10.925 | 13.84 | **0.12** | 13.73 |
| 5 | 12.776 | 19.96 | 2.24 | 17.72 |
| 8 | 12.921 | 22.18 | **4.70** | 17.48 |
| 15 | 13.077 | 22.17 | 4.63 | 17.54 |

서버 내부 span 은 **+60%** 오른다(13.84 → 22.17 µs). 분해가 그 정체를 짚는다 —
`admit`(제출 전 대기)이 0.12 → 4.70 µs 로 **39배**인 반면 전송·복호(`v2`)는
13.7 → 17.5 로 완만하다. **배치의 내부 비용은 거의 전부 "묶이기를 기다리는 시간"** 이다.

### 그런데 클라이언트가 보는 지연은 **오히려 줄어든다**

| chain | 처리량 | 서버 span | 클라 실측 avg |
|---:|---:|---:|---:|
| 1 | 10.93 M | 13.84 µs | 2.771 ms |
| 8 | 12.92 M | 22.18 µs | 2.331 ms |
| 16 | 13.02 M | 22.17 µs | **2.314 ms** |

span 이 8 µs 늘어난 건 사실이지만 **그것은 종단 지연의 1% 도 안 된다**(22 µs
대 2,314 µs). 지배적인 항은 큐 대기이고, 처리량이 오르면 큐가 빨리 빠진다.
서버측 `srv` 도 690 → 500 µs 로 같은 방향이다.

**따라서 배치는 상충이 아니라 양쪽 다 이득이다** — 처리량 +19.1%, 클라
지연 −16.5%. 포화는 **chain 8** 이고 그 이후는 둘 다 평평하다.

> 주의: 서버 내부 span 과 클라 종단 지연이 **반대로 움직인다.** 내부 지표만
> 보면 "배치는 지연을 판다" 로 읽히는데, 종단으로 보면 틀린 결론이다.

---

## 3. 동시성은 생각보다 일찍 포화한다

같은 결론이 두 축에서 독립적으로 나온다.

```text
O 축 (ORD 스윕, 발자국 고정)   ORD 4 가 ORD 8 의 98.6%
S 축 (곱 256, 발자국 동일)     16×16 12.493 · 32×8 12.348 · 64×4 12.193
                               128×2 11.171 · 256×1 10.270
```

**깊이 4 까지는 평평하고 그 아래로 급락한다.** S 축은 다섯 점의 wire 곱이
256, slots 가 512 로 **발자국이 완전히 같아** 형태만 비교되는 축인데,
깊이 4 이상에서는 QP 를 어떻게 나누든 −2.4% 안이고 깊이 2·1 에서 −17.8% 다.

즉 **"QP 를 몇 개 쓰느냐" 보다 "QP 당 깊이가 4 이상이냐" 가 중요하다.**

---

## 4. 파이프라인 깊이의 절벽 (`pipeline=512`, 쓰기 5%)

읽기 전용은 멀쩡한데 **쓰기가 조금 섞인 워크로드만 무너진다.**

```text
pipe 512   C(읽기만) 12.351   B(쓰기 5%) 5.053   A(쓰기 50%) 7.417
           B 에서 busy 23.7 — 워커 30 개 중 6 개가 논다
```

쓰기가 원인이면 A 가 더 나빠야 하는데 멀쩡하다. 0% 도 50% 도 아닌 **5% 만**
무너지는 것이 기전을 특정한다 — 동기 SET 이 파이프라인 배치 안에서
**앞을 막고 뒤를 줄 세운다**(head-of-line). 배치가 512 로 길고 막는 지점이
26 개쯤 흩어져 있을 때가 최악이고, 쓰기가 절반이면 어디를 잘라도 막혀
깊이가 더 나쁘게 만들 여지가 없다.

**다른 라운드·다른 클라이언트 빌드에서 재현됐다** — 처리량뿐 아니라
busy·span·srv 까지 2% 안에서 겹친다.

---

## 5. 형태 대 부하 — 둘 다 작동하고 비중이 다르다

`c × pipeline = 1024` 로 **총 미처리 요청을 고정**하고 깊이만 128배 바꾼 축이다.
Z/U 비만 읽는다.

```text
깊이        1024     512     256     128      64      32      16       8
YCSB-A    −80.8%  −74.7%  −77.7%  −78.4%  −79.9%  −78.6%  −76.0%  −72.6%
YCSB-B    −23.4%  −22.6%  −21.6%  −20.5%  −19.8%  −19.3%  −16.0%  −13.8%
```

**쓰기 절반이면 깊이가 아무것도 안 한다**(128배 범위에서 평평) — 순수 경합이다.
**쓰기 5% 면 깊이에 따라 10 포인트가 움직인다** — 여기서 HOL 이 얹힌다.

```text
경합(부하)   지배적. 바닥을 정한다
HOL(깊이)    부차적. 쓰기가 드물어 배치가 길 때만 얹힌다
```

---

## 6. 캠페인 중 발견해 고친 결함 (측정값을 바꾼 것들)

이 캠페인의 절반은 **측정 도구의 결함을 찾는 일**이었다.

| 결함 | 증상 | 영향 |
|---|---|---|
| `W` 가 큐 크기를 정함 | CQ = `2×W×nqp` 라 nqp² 로 증가 | **SET −12.3%**. nqp=64 무장 불가 |
| 전 워커 몫을 MR 하나로 | 게스트 커널 버디 상한 4 MB 에 걸림 | nqp=64 봉쇄 (실제로는 워커당 0.5 MB) |
| ORD 핀이 CM 파라미터로 | ORD>16 무장 불가, 256 은 0 으로 절단 | 계획 6점 소실 |
| ORD 협상값 무조건 채택 | 상대가 1 요청에 16 을 답해 핀이 지워짐 | O·S 축 전체가 무의미해질 뻔 |
| memtier upstream master | 구 커밋 대비 −22%, 간헐적으로 20% 덜 밂 | 라운드 6 첫 시도 전량 폐기 |

특히 **`W` 큐 사이징 결함은 라운드 3 의 결론 하나를 만들어냈다.** 당시
"QP 가 적을수록 낫다(+2.6%)" 가 나왔는데, CQ 가 `2×nqp²×ORD` 라 nqp=1 셀만
큐가 작았던 artifact 였다. 큐를 고친 라운드 6 에서는 nqp 1·2·4 가 구분되지
않는다(12.973 / 12.944 / 12.977).

---

## 7. 아직 열려 있는 것

```text
① item lock 이 원격 왕복을 감싸는지 코드로 확증          §1 의 기전
② ORD 16 에서의 하락이 ORD 탓인가 발자국 탓인가          진단 셀 R6-O16s64
③ 최적 구성의 명시 측정                                  깊이≥4 · chain 8 · nqp 무관
④ P 축 MIX 붕괴의 정확한 임계                            쓰기 비중 × 깊이 지도
⑤ value 축 상한 (slot=256 이라 d≤128)                    slot 확대는 −27.4% 대가
```

---

## 8. 이 결과를 어떻게 읽어야 하나

**균등 부하에서는 이 설계가 잘 동작한다.** 운영점에서 읽기 전용 12.9 M ops/s,
span 22 µs 다. 쓰기가 섞여도 YCSB-B 에서 11.5 M 이 나온다.

**편중 부하에서 쓰기가 많으면 벽에 부딪힌다.** 그리고 그 벽은 하드웨어나
대역폭이 아니라 **잠금 설계**에서 온다 — 그래서 고칠 수 있는 종류의 벽이다.
현실 워크로드가 대개 편중돼 있다는 점을 생각하면, 이것이 이 포트가 다음에
풀어야 할 문제다.
