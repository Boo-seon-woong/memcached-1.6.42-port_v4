# semi_final 결과 (라운드 3)

측정: 2026-08-06 ~ 08-07. 셀마다 GET / MIX(1:9) / SET 각 **180초**, 사이 20초.
고정: `slot=256 · W=wire곱 · EXT_READ_SLOTS=max(64, 2×wire곱) · admit_max=0`.
서버 `port_v4 c11ede3e`, 게스트 AMD SEV-SNP, 원격 메모리 `genie_memd 10.99.0.2`.

측정 완료 **68구성 204부하** (복제 3셀 + 가드 3셀 포함). 원자료:
`csv/SF3-*.csv`(축별) · `rows.tsv`(서버 42열) · `client.tsv`(memtier) ·
`genie/<cell>.txt`(memtier 표준출력 전문) · `ariel/arm/<cell>.txt`(무장 지문).

## 1. 관리자 요구 검증

> "각 실험마다 10M 이상 수준이 적어도 1개 이상의 값에서 발생하는 게 의도"

**8실험 전부 충족.** 각 축 최고점(GET):

```text
P 파이프라인  12.828M (P256)      E 클라×파이프  13.475M (2×512)
D 값 크기     13.717M (d=4)       C 체인         12.934M (C10)
Q nqp          13.096M (nqp=1)     O ORD          12.900M (ORD=8)
S 형태@곱256   12.539M (16×16)     T 스레드       12.808M (mcT=30)
```

계약(1:10 혼합 10M **and** GET-only 10M, 두 워크로드 span<30µs)도 운영점
`d=64`에서 성립한다 — GET 12.873M / MIX 10.687M / span 22.18µs.

## 2. 침대 재현성 — **0.9%**, 24시간 드리프트 없음

동일 구성 3쌍이 전부 1% 안이다.

```text
SF3-OP 12.873  vs  SF3-Q4    12.758    0.89%   (캠페인 중반)
SF3-OP 12.873  vs  SF3-OP-r3 12.988    0.89%   ← 드리프트 가드, 약 24 h 간격
SF3-O4 12.806  vs  SF3-O4-r2 12.863    0.45%
```

캠페인 시작과 끝이 0.89% 안이므로 **모든 축을 서로 비교해도 된다.**
판정 기준은 이 재현성 위에서 정한다 — 1% 미만은 구분 안 됨, 2% 이상은 효과.

## 3. 축별 결과

### 값 크기 D — 작을수록 단조 증가, 캠페인 최고점

```text
d      4      8      16     24     32     48     64     96     128
GET  13.717 13.609 13.529 13.204 13.346 12.790 12.492 11.998 11.469
MIX  11.112 11.188 11.053 10.915 10.930 10.671 10.452 10.104  9.657
SET   6.067  6.029  5.988  5.974  5.969  5.725  5.592  5.468  5.297
span  20.4   20.8   20.9    —      —      —      —      —      24.7
```

d=4→128 에서 −16.4%. slot=256 고정이라 전송 단위는 같은데도 준다 —
crypto 가 실바이트를 처리하는 몫이다. **급락 지점은 없다**(사전 예측 적중).

### 클라이언트×파이프라인 E — 같은 총부하 1024 에서 **−66.6%**

```text
형태  1×1024  2×512   4×256   8×128   16×64   32×32   64×16  128×8
GET   12.215  13.475  12.512  11.587   9.464   8.525   6.182  4.500
```

**곱이 같아도 형태가 3배를 가른다.** 정점 2×512 는 현 운영점(4×256)을
+7.7% 앞선다. 연결이 늘수록 이벤트 루프당 배치가 깨진다.

### 파이프라인 P — 256 에서 포화, 512 에서 MIX 붕괴

```text
pipe    1      8      32     64    128    256    384    512
GET   0.567  3.406  7.521  9.900 12.102 12.828 12.741 12.419
MIX   0.563  3.355  6.846  8.451 10.213 10.659  9.817  7.261
```

GET 은 256 이후 평평하지만 **MIX 는 384·512 에서 무너진다**(−31.9%).
SET 이 섞이면 깊은 파이프라인이 해가 된다 — 원인 미규명.

### 체인 C — 1→8 단조 상승, 8 이상 포화

```text
chain   1      2      3      4      5      6      7      8
GET  10.865 11.732 11.983 12.350 12.466 12.463 12.350 12.806
chain   9     10     11     12     13     14     15     16
GET  12.731 12.934 12.676 12.818 12.813 12.805 12.721 12.829
```

C1→C8 이 +17.9%. C8 이후는 전부 재현성(0.9%) 폭 안 — **체인 8 이 포화점**이다.

### ORD O — **깊이 1 이 절벽**

```text
ORD     1      2      4      8    협상16
GET   9.698 12.092 12.806 12.900 12.757     (O4 재측정 12.863)
span 327.28    —    22.95  22.12  22.35
```

깊이 1 은 처리량 −24.8% 에 span 327µs — **유일하게 계약을 깨는 구성**이다.
깊이 4 부터 포화. ORD>16 은 측정 불가(§5-1).

### nqp Q — 적을수록 낫다 (단조)

```text
nqp     1      2      4      8     16
GET  13.096 12.975 12.758 12.691 12.403
slots   64     64    128    256    512
```

nqp 1→16 에서 −5.3% 의 단조 감소. Q1↔Q2 는 발자국이 같은(slots=64) 통제쌍인데
0.93% 로 구분이 안 되니, 감소는 **nqp 자체보다 발자국 증가가 실어 나른다.**
다만 발자국 4배가 1.4% 밖에 안 되므로(§5-2) 둘 다 작은 항이다.
nqp=64 는 측정 불가(§5-3).

### 스레드 T — 16 까지 78% 효율, 이후 체감

```text
mcT     1      2      4      8     12     16     24     28     30
GET  0.751  1.539  2.983  5.475  7.498  9.360 11.385 12.583 12.808
스레드당 0.751 0.770 0.746 0.684 0.625 0.585 0.474 0.449 0.427
```

T1 대비 스레드당 처리량이 T30 에서 57% 로 떨어진다. 절대 처리량은 계속
늘지만 **8 스레드 이후 체감이 시작**된다.

### 형태@곱256 S — 2점만 측정 가능

```text
16×16  12.539     32×8  12.288     64×4  측정 불가(§5-3)
```

## 4. 형태 효과 — **확정**. 곱이 같아도 형태가 1.9% 를 가른다

곱과 발자국을 고정하고 형태만 바꾼 두 쌍. 복제로 군을 만들었다.

```text
wire 16 · slots 64            wire 256 · slots 512
  1×16   13.096  13.060         16×16   12.539  12.514
  4×4    12.806  12.863         32×8    12.288
  ─────────────────────         ─────────────────────
  군내 0.28% / 0.45%            군내 0.20% / (단일)
  차   +1.90%  겹침 없음        차   +1.94%  겹침 없음
```

두 쌍 모두 **군이 겹치지 않고**, 군내 폭이 군간 차의 1/4 이하이며,
차이가 재현성(0.9%)의 두 배다. wire 규모가 16 배, 발자국이 8 배 다른
두 지점에서 부호와 크기가 같다.

> **결론: wire 곱과 발자국이 같아도 QP 를 적게 쓰고 깊이를 주는 쪽이
> 1.9% 낫다.** 관리자가 실험 5·6 에 nqp·ORD 를 따로 명시하게 한 판단이
> 실측으로 옳았다 — 곱만으로는 구성을 특정할 수 없다.

한계: 세 번째 점 `S64x4` 가 측정 불가(§5-3)라 단조 추세 검정은 못 했고,
`S32x8` 은 단일 측정이다. 클라이언트 쪽 같은 원리는 E 축이 훨씬 큰
규모(−66.6%)로 보여준다.

## 5. 이 침대의 한계 4건

### (1) ORD>16 핀은 무장 불가 — 계획 6점 측정 불가

```c
extstore.c:644   .responder_resources = (uint8_t)ask, .initiator_depth = (uint8_t)ask
```

핀 값이 소프트 게이트가 아니라 **CM 협상 파라미터**로 들어간다. HCA 최대
16 을 넘는 depth 로 QP 가 서면 첫 READ 가 `remote invalid request (9)` 로
죽는다. `extstore.c:650` 주석의 "초과분은 SQ 에 쌓인다"는 실측으로 거짓이고,
`uint8_t` 캐스트라 ORD=256 은 0 으로 잘리기까지 한다.

측정 불가: `O32 O64 S1x256 S2x128 S4x64 S8x32`.
고치려면 CM 파라미터만 HCA 최대로 clamp 하고 소프트 게이트는 핀 값을 쓰면
된다(주석이 의도한 동작). **캠페인 중 바이너리 교체는 하지 않았다** —
지문 sha 가 바뀌면 앞선 셀 전부와 비교가 끊긴다.

### (2) 구동기 `SS` 누수 — T 축·OP-r2 발자국 4배, 영향 1.4%

`sf-resume.sh:187` 의 `SS` 는 평범한 셸 변수라 S 축 값(512)이 T 축과
OP-r2 로 남았다(`NQP`·`ORD` 는 env 접두라 무사).

```text
T30    slots 512   12.808      OP-r3  slots 128   12.988     1.4%
```

발자국 4배가 1.4% 이므로 T 축은 유효로 둔다. 저장소 구동기에는 `SS=128` 을
명시해 막았다. **부수 소득**: 발자국이 이 범위에서 거의 공짜라는 실측.

### (3) nqp=64 는 자원 한계로 무장 불가

```text
── 무장: … nqp=64 … (총 QP = 30 × 64)
extstore rdma_cm: create_cq failed (worker 0 qp 0): Cannot allocate memory
```

QP 1920 개에서 CQ 할당이 실패한다. 측정 불가: `Q64 · S64x4`.
S64x4 가 빠져 §4 의 단조 추세 검정이 불가능해졌다.

### (4) 축 CSV 분류 누락 — O 축이 `etc.csv` 로 (해결)

분류 사슬에 라운드 2 이름 `SF-O` 만 있고 `SF3-O` 가 없었다. 값은 온전했고
`tools/sf-split.py` 로 재분리했다.

## 6. 유실과 보충 — 오염 0건

보고된 셀은 **전부 3/3 워크로드**다. 반쪽 셀도, 틀린 조건으로 찍힌 값도 없다.

```text
보충 완료   C2 · C3 · C4    genie 사용한도 초과(2시간 무보고)로 창 소진
            D4              GO 도착 시 bed 불일치 → genie 정당 거부
            Q8              보고-먼저 순서 (genie 가 발사-먼저로 교정)
            O4-r2           형태 효과 복제용 (신규)
            OP-r3           드리프트 가드 (OP-r2 는 §5-2 로 무효)
측정 불가   O32 O64 S1x256 S2x128 S4x64 S8x32   (§5-1)
            Q64 S64x4                            (§5-3)
```

## 7. 관리자 결정 대기

```text
1. ORD>16 재개 — CM clamp 수정 후 6점 재측정 (바이너리 교체 필요)
2. nqp=64 재개 — mcT 를 낮춰 총 QP 를 줄이면 측정 가능하나 T 축과 교란된다
3. 운영점 이동 — E 축 2×512 (+7.7%), Q 축 nqp=1 (+2.6%)
4. P 축 MIX 붕괴(384·512, −31.9%) 원인 규명
```
