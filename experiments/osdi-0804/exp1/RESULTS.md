# exp1 결과 — local(stock) vs remote(port), **off-box** (2026-08-06)

계획 [`PLAN.md`](PLAN.md) · 그림 `img/osdi-20260806/f1-{A,B,C}.svg`, `f1b-memory-node-cpu.svg`

```text
측정      2026-08-06 야간, genie off-box 부하. 48 셀 (두 서버 × 21 uniform + 3 zipf)
지속      셀당 30초 (경향성 라운드). 램프 편향 −0.21% — 보정 없이 읽는다
stock     memcached 1.6.42 pristine 97ceee04, -t 30 -m 1024 -c 16384 -R 1024
          RDMA 미사용(로컬 메모리). LD_LIBRARY_PATH=$HOME/memtier (libevent)
port      c11ede3ebd2a45d8f32e9943, **EXT_RDMA_PROF=0** (계측 −4.2% 를 안 물린다)
          W=24 nqp=4 reap=8 chain=8 admit=64 inline, mcT=30
공통      memtier -t 30 -c 4 -d 64, keyspace 1 M, --distinct-client-seed
          uniform R:R / zipf Z:Z --key-zipf-exp=0.99
원본      ../../night-20260806/{client.tsv, rows.tsv, ariel/}, genie/ raw
```

> **이 파일이 exp1 의 표다.** `stock-colocated/` 는 genie 부재 중에 급히 잡은
> 예비 측정이고, 여기 값으로 대체됐다. 다만 폐기하지 않는다 — §4 의 교차
> 확인에 쓴다.

## 1. 워크로드별 (uniform, 마지막 행만 zipf θ=0.99)

### YCSB A — read 50 / update 50

| pipe | stock M ops/s | port M ops/s | 차이 | stock avg ms | port avg ms |
|---:|---:|---:|---:|---:|---:|
| 1 | 0.467 | 0.572 | **+22.5%** | 0.26 | 0.21 |
| 8 | 1.000 | 3.042 | **+204.2%** | 0.95 | 0.31 |
| 32 | 1.148 | 5.597 | **+387.5%** | 3.31 | 0.66 |
| 64 | 1.174 | 7.014 | **+497.4%** | 6.48 | 1.05 |
| 128 | 1.161 | 7.303 | **+529.0%** | 13.16 | 2.06 |
| 256 | 1.198 | 7.247 | **+504.9%** | 26.23 | 4.19 |
| 384 | 1.168 | 7.293 | **+524.4%** | 39.34 | 6.26 |
| 256 (zipf) | 1.100 | 1.734 | **+57.6%** | 27.84 | 17.63 |

### YCSB B — read 95 / update 5

| pipe | stock M ops/s | port M ops/s | 차이 | stock avg ms | port avg ms |
|---:|---:|---:|---:|---:|---:|
| 1 | 0.607 | 0.573 | **-5.6%** | 0.20 | 0.21 |
| 8 | 3.597 | 3.434 | **-4.5%** | 0.26 | 0.27 |
| 32 | 7.434 | 7.459 | **+0.3%** | 0.49 | 0.49 |
| 64 | 7.907 | 9.430 | **+19.3%** | 0.94 | 0.78 |
| 128 | 9.242 | 11.580 | **+25.3%** | 1.62 | 1.28 |
| 256 | 9.471 | 12.357 | **+30.5%** | 3.20 | 2.44 |
| 384 | 9.744 | 12.497 | **+28.3%** | 4.69 | 3.64 |
| 256 (zipf) | 6.540 | 9.381 | **+43.4%** | 4.65 | 3.23 |

### YCSB C — read 100

| pipe | stock M ops/s | port M ops/s | 차이 | stock avg ms | port avg ms |
|---:|---:|---:|---:|---:|---:|
| 1 | 0.611 | 0.571 | **-6.5%** | 0.19 | 0.21 |
| 8 | 4.020 | 3.485 | **-13.3%** | 0.23 | 0.27 |
| 32 | 9.874 | 8.083 | **-18.1%** | 0.35 | 0.45 |
| 64 | 11.497 | 10.155 | **-11.7%** | 0.62 | 0.71 |
| 128 | 14.001 | 12.932 | **-7.6%** | 1.04 | 1.14 |
| 256 | 15.372 | 13.827 | **-10.1%** | 1.95 | 2.17 |
| 384 | 15.918 | 14.014 | **-12.0%** | 2.84 | 3.24 |
| 256 (zipf) | 13.125 | 12.775 | **-2.7%** | 2.29 | 2.36 |

## 2. 읽으면 이렇다

> ### ⚠️ 이 표를 "원격 메모리가 로컬보다 빠르다" 로 읽으면 안 된다
>
> A 에서 port 가 6 배인 것은 사실이지만, **비교되고 있는 것은 메모리가 아니라
> 쓰기 경로다.** stock 은 SET 마다 슬랩 할당 + LRU 연결/해제 + `item_lock`
> 을 지나고, port 는 그 자리를 워커별 원격 슬롯 할당 + 비동기 완료로
> **갈아치웠다.** 원격 접근은 port 가 그 위에 얹어서 무는 비용이고, 그러고도
> 이긴 것이다.
>
> stock 의 1.2 M 천장이 진짜인지 세 가지로 확인했다 (co-located, 100 K 키, 20 초):
>
> ```text
> 서버 스레드   30 → 1.175 M    8 → 1.225 M      스레드를 줄이면 오히려 낫다
> 값 크기       8 B 1.263 M / 64 B 1.225 M / 512 B 1.167 M   크기와 거의 무관
> LRU 배경 끔   no_lru_maintainer 등 → 0.872 M   기본값이 이미 최선이다
> ```
>
> **셋 다 천장을 못 옮긴다** — 스레드도 바이트도 아닌 **op 당 직렬화**다.
> 다만 **그 직렬점을 아직 이름으로 지목하지 못했다**(perf 프로파일 필요).
> 그리고 두 SET 경로가 의미상 등가인지도 확인 안 됐다(축출 정책, ack 시점).
> **그 둘이 닫히기 전에는 이 행을 논문 주장으로 쓰지 않는다.**

**① 쓰기 비중이 부호를 뒤집는다.**

```text
C (쓰기 0%)    port −10~12%      원격화 대가가 정직하게 보인다
B (쓰기 5%)    port +30%         깊이 64 부터 역전
A (쓰기 50%)   port +505%        6 배
```

**② stock 의 쓰기 천장은 1.17 M 이고 깊이와 무관하다.** pipe 32 부터 평평하다
(1.148 → 1.198 → 1.168). 큐를 깊게 줘도, 스레드를 줄여도, 값을 8 B 로 줄여도
안 움직인다(위 상자). **op 당 직렬화**이며 그 지점은 아직 미규명이다 —
`item_lock`·슬랩·LRU 중 어디인지는 perf 로 지목해야 한다.

**③ 지연도 같은 방향이다.** A 의 pipe=256 에서 stock 26.23 ms 대 port 4.19 ms.
처리량이 6 배인데 지연이 6 배 낮다 — 같은 병목의 양면이다.

**④ skew 는 두 쪽 다 때리지만 순서를 안 바꾼다.**

```text
              uniform → zipf θ=0.99
stock C      15.372 → 13.125   −14.6%
port  C      13.827 → 12.775    −7.6%
stock A       1.198 →  1.100    −8.2%
port  A       7.247 →  1.734   **−76.1%**
```

port 의 A 가 skew 에서 크게 무너져 격차가 6 배 → 1.6 배로 좁는다. 서버측
창에서 그 순간 **busyCPU 가 11.9**(30 코어 중)였다 — 일이 없어서 노는 것이고
`badcrc` 가 3 → 128,570 으로 뛴다. 핫키 `item_lock` 경합이다.

## 3. 메모리 노드 CPU (F1b)

```text
genie_memd        0.00006 코어   66.6 h 동안 15.3 s (prefill 포함), op 당 0.40 ns
genie 박스 전체    27.9 코어      memtier 부하 생성기 (48 코어의 58.2%)
guest memcached   29.9 코어      port 서버 (30 코어의 99.7%)
```

one-sided READ 라 **메모리 노드가 서버의 50 만분의 1** 을 쓴다.

## 4. co-located 예비 측정과의 교차 확인

`stock-colocated/` 는 부하 생성기를 게스트 안에 둔 것이라 −31.7% 를 물 것으로
봤다. 실제로는 **워크로드마다 달랐고, 그 차이가 §2-② 를 뒷받침한다.**

```text
              co-located    off-box     차이
stock C-P256    11.220      15.372     +37.0%   ← CPU 를 나눠 쓴 대가가 그대로
stock A-uni      1.188       1.198      +0.8%   ← 대가가 없다
```

**A 는 co-location 대가를 안 문다.** CPU 가 병목이 아니라 락이 병목이니
CPU 를 나눠 써도 손해가 없는 것이다. 예비 측정이 그때 남긴 "A 는 메모리
배치가 아니라 락 경합을 재게 된다"는 각주가 이 두 줄로 확인됐다.

## 5. 이 표에 없는 것

```text
stock SET-only        exp1 은 A/B/C 뿐이다. 쓰기 경로 비교의 가장 깨끗한 짝이
                      빠져 있다 — 다음 라운드 1 순위
stock perf 프로파일    1.2 M 직렬점을 심볼로 지목해야 A 행을 주장으로 쓸 수 있다
두 SET 경로의 등가성   축출 정책·ack 시점이 같은지 확인 안 됐다
real-world 키 분포   자산 없음 (DPA-Store 사용분) — PENDING 차단 목록
60초 × 3 런         오늘은 30초 단발이다. 절대값·최종 그림은 본측정에서
YCSB D/E/F          memtier 로 표현 불가(D latest·E scan) 또는 부분(F RMW)
```
