diff --git a/memtier_benchmark.cpp b/memtier_benchmark.cpp
index 5ed3ddb..73fa744 100644
--- a/memtier_benchmark.cpp
+++ b/memtier_benchmark.cpp
@@ -743,6 +743,7 @@ static void config_print_to_json(json_handler *jsonhandler, struct benchmark_con
     jsonhandler->write_obj("key_stddev", "%f", cfg->key_stddev);
     jsonhandler->write_obj("key_median", "%f", cfg->key_median);
     jsonhandler->write_obj("key_zipf_exp", "%f", cfg->key_zipf_exp);
+        jsonhandler->write_obj("key_zipf_scramble", "%s", cfg->key_zipf_scramble ? "true" : "false");
     jsonhandler->write_obj("reconnect_interval", "%u", cfg->reconnect_interval);
     jsonhandler->write_obj("retry_on_error", "\"%s\"", cfg->retry_on_error ? "true" : "false");
     jsonhandler->write_obj("max_retries", "%d", cfg->max_retries);
@@ -1157,6 +1158,7 @@ static int config_parse_args(int argc, char *argv[], struct benchmark_config *cf
         o_key_stddev,
         o_key_median,
         o_key_zipf_exp,
+        o_key_zipf_scramble,
         o_show_config,
         o_hide_histogram,
         o_print_percentiles,
@@ -1285,6 +1287,7 @@ static int config_parse_args(int argc, char *argv[], struct benchmark_config *cf
         {"key-stddev", 1, 0, o_key_stddev},
         {"key-median", 1, 0, o_key_median},
         {"key-zipf-exp", 1, 0, o_key_zipf_exp},
+        {"key-zipf-scramble", 0, 0, o_key_zipf_scramble},
         {"reconnect-interval", 1, 0, o_reconnect_interval},
         {"reconnect-on-error", 0, 0, o_reconnect_on_error},
         {"max-reconnect-attempts", 1, 0, o_max_reconnect_attempts},
@@ -1723,6 +1726,9 @@ static int config_parse_args(int argc, char *argv[], struct benchmark_config *cf
                 return -1;
             }
             break;
+        case o_key_zipf_scramble:
+            cfg->key_zipf_scramble = true;
+            break;
         case o_key_zipf_exp:
             endptr = NULL;
             cfg->key_zipf_exp = strtod(optarg, &endptr);
@@ -2813,6 +2819,9 @@ void usage()
         "      --key-median               The median point used in the Gaussian distribution\n"
         "                                 (default is the center of the key range)\n"
         "      --key-zipf-exp             The exponent used in the zipf distribution, limit to (0, 5)\n"
+        "      --key-zipf-scramble        Scatter the zipf ranks over the key range with an FNV-1a\n"
+        "                                 hash, as YCSB's ScrambledZipfian does. Without it the hot\n"
+        "                                 keys are literally the lowest key IDs.\n"
         "                                 Higher exponents result in higher concentration in top keys\n"
         "                                 (default is 1, though any number >2 seems insane)\n"
         "\n"
@@ -5087,7 +5096,7 @@ int main(int argc, char *argv[])
             // user can't specify 0.0, so 0.0 means unset
             cfg.key_zipf_exp = 1.0;
         }
-        obj_gen->set_key_zipf_distribution(cfg.key_zipf_exp);
+        obj_gen->set_key_zipf_distribution(cfg.key_zipf_exp, cfg.key_zipf_scramble);
     }
 
     // Prepare output file
diff --git a/memtier_benchmark.h b/memtier_benchmark.h
index 9348a05..fa929db 100644
--- a/memtier_benchmark.h
+++ b/memtier_benchmark.h
@@ -149,6 +149,7 @@ struct benchmark_config
     double key_stddev;
     double key_median;
     double key_zipf_exp;
+    bool key_zipf_scramble;
     const char *key_pattern;
     unsigned int reconnect_interval;
     bool reconnect_on_error;
diff --git a/obj_gen.cpp b/obj_gen.cpp
index 08cbe4c..86b1570 100644
--- a/obj_gen.cpp
+++ b/obj_gen.cpp
@@ -155,6 +155,7 @@ object_generator::object_generator(size_t n_key_iterators /*= OBJECT_GENERATOR_K
         m_key_zipf_Hmin(0),
         m_key_zipf_Hmax(0),
         m_key_zipf_s(0),
+        m_key_zipf_scramble(false),
         m_key_buffer(NULL),
         m_key_buffer_size(0),
         m_value_buffer(NULL),
@@ -190,6 +191,7 @@ object_generator::object_generator(const object_generator &copy) :
         m_key_zipf_Hmin(copy.m_key_zipf_Hmin),
         m_key_zipf_Hmax(copy.m_key_zipf_Hmax),
         m_key_zipf_s(copy.m_key_zipf_s),
+        m_key_zipf_scramble(copy.m_key_zipf_scramble),
         m_key_buffer(NULL),
         m_key_buffer_size(0),
         m_value_buffer(NULL),
@@ -329,10 +331,12 @@ void object_generator::set_key_distribution(double key_stddev, double key_median
 }
 
 // should be called after set_key_range in memtier_benchmark.cpp
-void object_generator::set_key_zipf_distribution(double key_exp)
+void object_generator::set_key_zipf_distribution(double key_exp, bool scramble)
 {
     const double eps = 1e-4;
 
+    m_key_zipf_scramble = scramble;
+
     if (key_exp < eps)
         m_key_zipf_exp = 0.;
     else if (fabs(key_exp - 1) < eps)
@@ -388,7 +392,28 @@ unsigned long long object_generator::normal_distribution(unsigned long long r_mi
 // ACM Transactions on Modeling and Computer Simulation.
 // Volume 6 Issue 3 July 1996 pp 169–184
 // https://doi.org/10.1145/235025.235029
+/* YCSB Utils.fnvhash64 와 같은 FNV-1a 64. 순위를 키공간에 흩뿌리는 데만 쓴다. */
+static unsigned long long fnvhash64(unsigned long long val)
+{
+    unsigned long long h = 0xCBF29CE484222325ULL;
+    for (int i = 0; i < 8; i++) {
+        h ^= (val & 0xff);
+        h *= 0x100000001b3ULL;
+        val >>= 8;
+    }
+    return h;
+}
+
 unsigned long long object_generator::zipf_distribution()
+{
+    unsigned long long k = zipf_rank();
+    if (!m_key_zipf_scramble)
+        return k;
+    unsigned long long n = m_key_zipf_max - m_key_zipf_min + 1;
+    return m_key_zipf_min + fnvhash64(k - m_key_zipf_min) % n;
+}
+
+unsigned long long object_generator::zipf_rank()
 {
     const double eps = 1e-4;
 
diff --git a/obj_gen.h b/obj_gen.h
index f9594c5..70e36b9 100644
--- a/obj_gen.h
+++ b/obj_gen.h
@@ -106,6 +106,10 @@ protected:
     double m_key_zipf_Hmin;
     double m_key_zipf_Hmax;
     double m_key_zipf_s;
+    // YCSB ScrambledZipfian 대응: 순위를 FNV 해시로 키공간에 흩뿌린다.
+    // 없으면 핫 키가 key_min 부터 연속이라, 키 번호와 저장 위치가 상관된
+    // 백엔드에서는 핫 데이터가 한곳에 몰린다.
+    bool m_key_zipf_scramble;
 
     std::vector<unsigned long long> m_next_key;
 
@@ -132,6 +136,7 @@ public:
     unsigned long long normal_distribution(unsigned long long r_min, unsigned long long r_max, double r_stddev,
                                            double r_median);
     unsigned long long zipf_distribution();
+    unsigned long long zipf_rank();
 
     void set_random_data(bool random_data);
     void set_data_size_fixed(unsigned int size);
@@ -142,7 +147,7 @@ public:
     void set_key_prefix(const char *key_prefix);
     void set_key_range(unsigned long long key_min, unsigned long long key_max);
     void set_key_distribution(double key_stddev, double key_median);
-    void set_key_zipf_distribution(double key_exp);
+    void set_key_zipf_distribution(double key_exp, bool scramble = false);
     void set_random_seed(int seed);
     void fill_value_buffer();
     unsigned long long get_key_index(int iter);
