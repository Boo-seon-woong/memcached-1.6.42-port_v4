# exp2 — 성능 축(a) + 지연 분해(b) + 커넥션 교환(c) + E0

## E0 — `ext_drain_empty_max` 확정 (PENDING 블록 1, **모든 측정보다 먼저**)

코드 주석이 "측정으로 정한다"(memcached.c:267)인 채 0(무중단 스핀)으로
남아 있고, 저부하 CPU 의 대부분이 스핀이다(pipe=1 에서 busy 22.3/30 인데
0.575 M). 이 값이 exp2(b) 저부하 수치에 그대로 실린다.

```text
셀    E0-{0,4,16,64}-P{1,32}     GET-only, 30초, 값마다 서버 재기동     8 부하
판정  pipe=32 처리량 −1% 이내인 후보 중 pipe=1 busyCPU 최소값. 동률이면 큰 값
```

### 결과 (2026-08-06, 빌드 c11ede3e) — **DEM = 0 유지로 종결**

| DEM | pipe=32 처리량 | 대비 | pipe=1 처리량 | pipe=1 busy | pipe=1 CPU/op | 판정 |
|---:|---:|---:|---:|---:|---:|---|
| **0** | **7.913 M** | 기준 | 0.552 M | **21.9** | 39.7 µs | **채택** |
| 4 | 7.271 M | −8.11% | 0.571 M | 22.2 | 38.9 µs | 탈락 |
| 16 | 7.466 M | −5.65% | 0.546 M | 22.1 | 40.5 µs | 탈락 |
| 64 | 7.548 M | −4.61% | 0.578 M | 22.0 | 38.3 µs | 탈락 |

처리량은 genie 클라이언트값, busy·CPU/op 는 ariel 창. 후보 셋 전부 −1% 밖이라
판정 기준의 첫 조건에서 탈락하고, 둘째 조건(pipe=1 busyCPU 최소)도 0 이 이긴다.

**저부하에서 얻는 것이 없다는 것이 이 블록의 실질 결과다.** CPU/op 가
39.7 / 38.9 / 40.5 / 38.3 으로 단조하지 않고 창 편차(σ 1.0%) 안에서 흔들린다 —
"저부하 CPU 의 대부분이 스핀"이라는 전제가 이 노브로는 확인되지 않았다.
반대로 고부하 손실은 부호가 일정하고 공격성에 단조(−8.11 → −4.61%)다.
`memcached.c:267` 의 "측정으로 정한다" 주석은 이 표로 닫혔다.

> 저부하 CPU 가 어디서 나오는지는 **여전히 미해결**이다. `ext_drain_spin=1024`
> 자체를 낮추는 축은 안 봤다 — DEM 은 "연속 빈 poll" 조건이고 spin 상한과
> 다른 노브다. 후속 축으로 남긴다.

## (a) 축별 성능 — **신규 셀 없음. 조립 명세다**

throughput-latency 곡선의 offered-load 축은 `pipeline` 이고, `mcT`/`nqp` 는
점 가족이다. 전부 기존·대기 캠페인이 생산한다:

| 축 | 데이터 소스 | 상태 |
|---|---|---|
| pipeline 곡선 (PROF=0) | exp1 PT-P{1..384} 7점 | PENDING 블록 7 |
| pipeline 곡선 (PROF=1, 세분 포함) | shape C1 스윕 | 블록 2 에서 C1-128/256 창 보충 |
| mcT(=mtT) 확장 — SMART Fig.13 | shape C2 6셀 | PENDING 블록 2 |
| nqp·형태 (곱 16/32/64/128) | shape A 계열 | 〃 |
| QP 수 × depth=1 | shape B 7셀 | 〃 |

## (b) 클라이언트 지연 분해 — XSTORE Fig.12b 는 **저부하** 조건

정본은 기측정 pipe=1 분해(207.20 µs — que 0.34 / pre 2.45 / span 17.88
[admit 7.00 / xfer 8.13 / crypto 1.61 / sync 0.05] / post 23.62; 저부하에서도
post 가 xfer 의 2.9 배). 운영점 2,277.58 분해는 보조 그림.

```text
추가 1셀  BD-PIPE-1-r2: E0 확정값으로 pipe=1 재측정 (PROF=1, 30초)
          — 스핀 정책이 저부하 분해를 바꾸는지 확인. 바뀌면 r2 가 정본
```

## (c) 커넥션 ↔ 깊이 교환 (N=7,680 고정) — 6 부하

전 캠페인이 `-c 4` 단일이라 이 축은 데이터가 0 이다.

```text
셀    E2C-{c4p256*, c16p64, c64p16} × {GET-only, 1:9}     30초
      * c4p256 은 기존 운영점 값 재사용 가능하면 생략
서버  운영값 불변 (클라이언트만 변경)
판정  memtier 실측 latency 로만 — 커넥션을 늘리면 대기가 read() 이전(커널
      소켓 버퍼)으로 옮겨가 srv 지표는 개선처럼 보인다. 서버 지표 판정 금지
예측  직렬화 큐 감소(지연↓) vs syscall 상각 감소(처리량↓) — 부호 미지,
      어느 쪽이든 결과가 논문 §분해의 근거
```
